# 子代理

模型通过 `task` 工具把独立子任务派发给子代理(子代理有自己的系统提示词,跑完整工具循环后汇报结果)。

- 内置:**general-purpose**(通用)、**Explore**(只读检索,不许改文件)。
- 设置 > 子代理 可创建自定义:名称(英文标识)+ 一句话描述(供模型选择时参考)+ 系统提示词。保存在应用数据目录 `agents/*.md`。
- `/agent` 列出全部;`/agent <名称> <任务>` 直接派发,如 `/agent Explore 看看 src 目录里哪些文件超过 300 行`。

