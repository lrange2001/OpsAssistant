# 检查点与回滚

- 模型每次**修改文件之前**自动保存检查点(快照存在应用数据目录,不依赖 git),保存时右上有 toast 提示。
- `/rewind status` 列出最近 10 个检查点(时间、标签、文件数)。
- `/rewind latest` 或 `/rewind <id>` 把文件恢复到该检查点(新写入的文件会被移除)。**对话本身不变**,只回滚文件。
- `/fork <id|latest>` 先恢复检查点再分叉会话,原会话原样保留。
- 状态浮层(左下角 checkpoints 徽章)里也能一键 Restore。

