# 会话管理

- 顶栏时钟图标打开历史:**Pinned / Recent / Archived** 三个分区,顶部搜索框可搜标题和消息内容。
- 悬停条目:pin 置顶、arch 归档、x 删除(二次确认)。**双击重命名**。
- 会话标题:第一轮完成后由模型自动生成简短标题(也可手动改)。
- 最多保留 30 个(超出的最旧的挤掉)。
- 分叉的会话标注 "(fork)"。
- 生成中的会话条目带运行标记(running);多个会话可并行生成,见"对话与输入 > 多会话并行生成"。
- Cmd+Shift+[ / ] 在会话间快速切换。

